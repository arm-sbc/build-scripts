# build-scripts
Firmware build script for ARM-SBC boards.
Plaese use ubuntu 22 for building.
The script will install necessary dependencies for compilation, however there might some challenges depends on the installed packages, if the build fails 
install the missing dependencies.

[git clone https://github.com/arm-sbc/build-scrpits.git.](https://github.com/arm-sbc/build-scripts.git)

./set_env.sh)

